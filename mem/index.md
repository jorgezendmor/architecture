# Project Memory

## Core
Visual Edit image swaps: copy uploaded file byte-for-byte. Never re-encode, resize, convert format, or run PIL/imagemagick/cwebp. Keep original extension; if it differs, update the import path. Re-encoding destroys thin line weights.
Never add borders/outlines/rings around displayed images (figures or thumbnails).
